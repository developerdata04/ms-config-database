server:
  port: 91

# Database
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource: 
     driver-class-name: com.mysql.cj.jdbc.Driver
     url: jdbc:mysql://db-mysql-service:3306/db_producto?serverTimezone=GMT-6
     username: usu_producto
     password: 123456
     
  boot:
    admin:
       client:
         url: http://app-admin-service:8887
         instance:
            management-base-url: http://localhost:91
# Eureka Discovery Client

eureka:
   client:
      fetch-registry: true
      serviceUrl:
         defaultZone: http://app-registro-service:8889/eureka
         
#

#Metrics related configurations
management:
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
    gateway:
      enabled: true   
  endpoints:
      web:
        exposure:
          include: "*"
      health:
        show-details: always
  metrics:
    export:
      prometheus:
        enabled: true