server:
  port: 94

# Database
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource: 
     driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
     url: jdbc:sqlserver://mssql-deployment:1433;databaseName=db_movimiento;encrypt=true;trustServerCertificate=true;
     username: sa
     password: galaxy2023#
     
  boot:
    admin:
       client:
         url: http://app-admin-service:8887
         instance:
            management-base-url: http://localhost:94
# Eureka Discovery Client

eureka:
   client:
      fetch-registry: true
      serviceUrl:
         defaultZone: http://app-registro-service:8889/eureka
         
#

#Metrics related configurations
management:
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
    gateway:
      enabled: true   
  endpoints:
      web:
        exposure:
          include: "*"
      health:
        show-details: always
  metrics:
    export:
      prometheus:
        enabled: true